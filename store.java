package hashtagManagement;

import java.util.*;

public class store {
    // defining the hashmaps for storing the counts, active users and posts.
    public static HashMap<String, List<String>> hashtagPosts = new HashMap<>();

    public static HashMap<String, Integer> hashsCount = new HashMap<>();

    public static HashMap<String, Integer> user_active = new HashMap<>();

}